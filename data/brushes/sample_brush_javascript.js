
/*

	simple as can be.
	the wash host application provides arrays of fixed lengths:
	points_x
	points_y
	pressure
	rotation
	tilt_x
	tilt_y

	and some others TBD
	probably will provide seeded randoms as well and maybe noise

	probably will add some better type checking and safety later too, idk!

*/


/*
var stroke = {};

function create_stroke(data)
{
	var i, n;
	for ( i = 0 , n = data.length; i < n; i++)
	{
		data[i] += 5;
	}
	return data;

}

test_data = [1,2,3];

var data = create_stroke(test_data);
writeln("whee " + data[0]);

for ( var i in this )
{
	writeln(i);

}
writeln("////////////////////////?")
*/




/*
if ( myArr )
{
	write("myArr: " + myArr.length);
	//var str = new TextDecoder().decode(myArr);
}
*/

//write("hello");


//writeln(myArr);
